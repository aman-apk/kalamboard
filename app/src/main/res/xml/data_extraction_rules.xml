<?xml version="1.0" encoding="utf-8"?>
<!--
    Offline build: no user data may ever leave this device.

    android:allowBackup="false" already disables cloud backups on all supported API levels,
    but on Android 12+ (API 31) it does NOT disable device-to-device (D2D) transfer. These
    rules therefore exclude every storage domain from BOTH channels, so that neither the
    settings datastore, the installed extensions, nor the personal user dictionary
    (floris_user_dictionary) can be extracted off-device by the platform.
-->
<data-extraction-rules>
    <cloud-backup>
        <exclude domain="root" path="."/>
        <exclude domain="file" path="."/>
        <exclude domain="database" path="."/>
        <exclude domain="sharedpref" path="."/>
        <exclude domain="external" path="."/>
        <exclude domain="device_root" path="."/>
        <exclude domain="device_file" path="."/>
        <exclude domain="device_database" path="."/>
        <exclude domain="device_sharedpref" path="."/>
    </cloud-backup>
    <device-transfer>
        <exclude domain="root" path="."/>
        <exclude domain="file" path="."/>
        <exclude domain="database" path="."/>
        <exclude domain="sharedpref" path="."/>
        <exclude domain="external" path="."/>
        <exclude domain="device_root" path="."/>
        <exclude domain="device_file" path="."/>
        <exclude domain="device_database" path="."/>
        <exclude domain="device_sharedpref" path="."/>
    </device-transfer>
</data-extraction-rules>
